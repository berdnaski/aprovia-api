services:
  db:
    container_name: aprovai-db
    image: postgres:15
    restart: always
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: db_aprovai
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

  redis:
    container_name: aprovai-redis
    image: redis:alpine
    restart: always
    ports:
      - "6379:6379"

volumes:
  db_data:
